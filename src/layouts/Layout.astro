
const { title = "Danny Tour and Travels | The Full Experience" } = Astro.props;

<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Danny Tour and Travels - The Full Experience. Gorilla trekking, Car Hire, Honeymoon packages, and School Tours in Uganda." />
  <title>{title}</title>
  <link rel="icon" href="/images/logo1.jpeg" type="image/jpeg" />

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet" />

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />

  <!-- If you want to extract custom CSS later → create src/styles/global.css and do: -->
  <!-- import '../styles/global.css'; -->

  <slot name="head" />
</head>

<body>
  <nav id="mainNavbar" class="navbar navbar-expand-lg fixed-top glass-navbar">
    <div class="container">
      <a class="navbar-brand fw-bold" href="#">
        <img src="/images/logo1.jpeg" alt="Logo" width="30" height="30" class="me-2 rounded" />Danny Tour and Travels
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link active" aria-current="page" href="#home">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="#about">About</a></li>
          <li class="nav-item"><a class="nav-link" href="#tours">Activities</a></li>
          <li class="nav-item"><a class="nav-link" href="#car-hire">Car Hire</a></li>
          <li class="nav-item"><a class="nav-link" href="#trust">Why Us</a></li>
          <li class="nav-item"><a class="nav-link" href="#inquiry">Inquire</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <slot />  <!-- ← page content goes here -->

  <footer class="bg-dark text-white py-5 border-top border-secondary">
    <!-- ... your full footer HTML remains almost identical ... -->
    <div class="container">
      <!-- ... -->
      <div class="text-center text-secondary">
        <p>&copy; <span id="currentYear">2025</span> Danny Tour and Travels. All rights reserved.</p>
      </div>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

  <!-- Modal stays here or can be moved to a component later -->
  <div class="modal fade" id="carGalleryModal" tabindex="-1" aria-hidden="true">
    <!-- ... your modal HTML ... -->
  </div>

  <script is:inline define:vars="{ currentYear: new Date().getFullYear() }">
    // Almost your original script, just small tweaks
    document.addEventListener('DOMContentLoaded', () => {
      const navbar = document.getElementById('mainNavbar');
      const setNavbarScrolled = () => {
        if (window.scrollY > 50) navbar?.classList.add('scrolled');
        else navbar?.classList.remove('scrolled');
      };
      setNavbarScrolled();
      window.addEventListener('scroll', setNavbarScrolled, { passive: true });

      // ... rest of your logic: close mobile menu, carousel modal, reveal observer, form filled, active nav link ...
      // year is already in footer via JS or you can do:
      document.getElementById('currentYear').textContent = currentYear;

      // ... keep the rest mostly unchanged ...
    });
  </script>

  <slot name="after-body" />
</body>
</html>
